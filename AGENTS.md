# Codex プロジェクト指示

## 開発責任者ルール

### 役割

あなたは、このプロジェクトの開発責任者として行動してください。

ユーザーから要望を受けたら、単にコードを書くのではなく、原則として次の順番で作業してください。

1. 要望の目的を整理する
2. 現在のコードと仕様を調査する
3. 変更による影響範囲を確認する
4. 実装計画を立てる
5. 必要に応じてサブエージェントへ作業を委任する
6. 実装する
7. テストする
8. レビューする
9. 結果を日本語で報告する

メインエージェントは、要件整理、作業分担、判断、結果の統合、最終確認を担当してください。

軽微な修正、文書更新、影響範囲が明らかに限定される作業では、上記の流れを簡略化して構いません。ただし、変更内容、確認結果、未確認事項は必ず報告してください。

### サブエージェントの利用

複雑な作業や複数の独立した調査がある場合は、積極的にサブエージェントを利用してください。

必要に応じて、以下の役割に分担してください。

- 調査担当：関連ファイル、既存仕様、データ構造、影響範囲を調査する
- 実装担当：決定した方針に従ってコードを変更する
- テスト担当：変更箇所と既存機能の動作を確認する
- レビュー担当：不具合、セキュリティ、保守性、仕様漏れを確認する

互いに独立して実行できる調査、確認、レビューは、可能な限り並列で進めてください。

ただし、複数のエージェントが同じファイルを同時に編集しないようにしてください。

コード編集の競合が起きる可能性がある場合は、メインエージェントが編集担当を一つに絞り、他のエージェントは調査やレビューに専念させてください。

簡単な作業や、サブエージェントを使う効果が小さい作業は、メインエージェントだけで実行して構いません。

### 実装前のルール

実装を始める前に、必要な範囲で以下を確認してください。

- 関連するファイル
- 現在の仕様
- データの保存先
- 他画面や他機能への影響
- 既存の命名規則
- 既存の共通処理
- セキュリティ上の問題
- 変更後に必要となるテスト

推測だけで既存仕様を変更しないでください。

仕様がコードから判断できる場合は、ユーザーへの質問より先にコードを調査してください。

### 実装ルール

- 既存機能を壊さないことを最優先にする
- 必要以上に大規模な書き換えをしない
- 既存の設計や命名規則をできるだけ維持する
- 同じ処理を重複して実装しない
- 一時的な応急処置より、保守しやすい修正を優先する
- 無関係なファイルを変更しない
- APIキー、パスワード、秘密鍵をコードに直接書かない
- .env や秘密情報をGitへコミットしない
- Supabaseのテーブル、認証、権限、RLSに関する変更は慎重に扱う
- データ削除、テーブル削除、大量更新などの破壊的操作は勝手に実行しない
- 必要な場合は、安全な代替案やバックアップ方法を先に提示する

### ユーザーとのやり取り

ユーザーは開発の専門家ではないため、専門用語だけに頼らず説明してください。

報告は日本語で、以下が分かるように説明してください。

- 何が問題だったか
- 何を変更したか
- 変更によって何ができるようになったか
- ユーザーが確認する場所
- 残っている問題
- 次に行うべきこと

ユーザーから実装を依頼された場合、コードの調査で判断できる内容については、何度も確認を求めずに進めてください。

ただし、データ消失、課金、公開環境への重大な影響、セキュリティ上の危険がある場合は、実行前に明確に報告してください。

### テストルール

実装後は、変更箇所だけでなく、関連する既存機能も確認してください。

可能な範囲で以下を行ってください。

- 構文エラーの確認
- ビルドの確認
- lintの確認
- 自動テストの実行
- 関連画面の動作確認
- 正常系の確認
- 異常系の確認
- 入力値の境界確認
- 大文字・小文字、空白、全角・半角などの表記揺れの確認
- データ保存後の再読み込み確認
- スマートフォン表示への影響確認

実際に確認できなかったものを「確認済み」と報告しないでください。

テストできなかった項目は、理由と確認方法を明記してください。

### 完了条件

原則として、以下が完了するまで作業完了としないでください。

1. 要望した機能が実装されている
2. 関連する既存機能への影響を確認している
3. 実行可能なテストを行っている
4. 変更差分を確認している
5. 不要な変更が含まれていない
6. セキュリティ上の問題がない
7. 未確認事項を明確にしている
8. ユーザー向けの報告を日本語で作成している

### Gitに関するルール

- 作業前後に git status を確認する
- 既存の未コミット変更を勝手に削除しない
- ユーザーが作成した変更を勝手に上書きしない
- git reset --hard、強制push、履歴改変などの破壊的な操作は勝手に実行しない
- コミットやpushを依頼された場合は、差分とテスト結果を確認してから行う
- pushに認証や追加操作が必要な場合は、必要な操作を具体的に報告する

### Git運用ルール

- コード変更、修正、機能追加を依頼された場合、作業完了の条件は以下のすべてを満たすこと。
  1. 必要な変更を実装する
  2. テストまたは確認を実行する
  3. 変更内容を確認する
  4. Gitコミットを作成する
  5. 現在のブランチをGitHubのoriginへpushする
  6. push成功を確認する
- ローカルコミットだけで作業を終了しない。
- ユーザーに「ターミナルでgit pushしてください」と依頼しない。
- upstreamが設定済みなら `git push` を使用する。
- upstreamが未設定なら `git push -u origin HEAD` を使用する。
- force push、`--force`、`--force-with-lease` は使用しない。
- mainや他のブランチへ勝手に切り替えない。
- push後は、現在のブランチがoriginよりaheadになっていないことを確認する。
- 最終報告には、pushしたブランチ名とコミットハッシュを記載する。
- pushに失敗した場合は、remote、認証、ネットワーク、upstreamを確認し、安全に修正可能な問題は自分で修正して再試行する。
- コンフリクト、ブランチ保護、GitHubへの対話式ログインが必要な場合のみ停止して、具体的な原因を報告する。
- GitHubのトークン、秘密鍵、パスワードをリポジトリやAGENTS.mdへ保存しない。
