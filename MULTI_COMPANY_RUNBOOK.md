# 複数社対応 安全適用手順

## 現在の状態

コードとSQLは実装済みですが、本番Supabaseにはまだ適用しません。`multi_company_ready()` が存在しない、または `false` の間は、既存画面は従来動作を維持し、会社管理・利用者招待は無効になります。

2社目は、以下の分離テストがすべて終わるまで作成しないでください。

## 役割

- `system_admin`: 全会社の作成・切り替え、会社管理者と作業者の招待
- `company_admin`: 所属会社の工数・マスタ管理、所属会社の作業者招待
- `worker`: 所属会社の生産番号・作業内容を参照し、自分の工数だけを入力・閲覧・編集

一般公開の自由登録は行いません。会社と最初の会社管理者はシステム管理者が作り、以後の作業者は会社管理者が招待します。

## 本番適用前の必須準備

1. 本番DBのバックアップ方法と復元可否を確認する。
2. 工数・全マスタ・`user_profiles` を会社単位で退避できるようにする。
3. 別Supabaseプロジェクトへ本番相当のスキーマと匿名化データを用意する。
4. Vercel Previewでこのブランチの画面を確認する。
5. Supabase Authの公開サインアップを無効のままにする。

## テスト環境での適用順

### 1. 準備migration

`supabase/migrations/202608180001_multi_company_prepare.sql` を実行します。会社列と機能フラグを追加しますが、会社分離はまだ有効になりません。

### 2. 既存会社を1件作成

SQL Editorで既存利用会社を `company_master` に1件追加し、返されたUUIDを控えます。実際の会社名を使い、同名の重複がないことを確認してください。

### 3. 変更前確認

`SUPABASE_MULTI_COMPANY_CONFIRM_ONLY.sql` を実行します。このSQLは読み取りだけです。空の会社ID、不整合、会社内の重複、ユーザー紐づけを確認します。

### 4. 既存データへ会社IDを設定

`SUPABASE_MULTI_COMPANY_BACKFILL_TEMPLATE.sql` の `target_company_id` を手順2のUUIDへ置き換えて実行します。NULLの会社IDだけを埋め、削除・統合は行いません。不整合があればトランザクション全体が取り消されます。

### 5. 再確認

確認専用SQLを再実行し、次を確認します。

- 全対象テーブルの `missing_company` が0件
- 会社をまたぐ工数参照が0件
- 会社内の作業者名・作業内容名・生産番号キー重複が0件
- 会社内の元請け名・有効な単価設定の重複が0件
- 同じAuthユーザーのプロフィール重複、同じ作業者に対する有効プロフィール重複が0件
- workerの `user_profiles.company_id` と `worker_master.company_id` の不一致が0件
- この時点の `multi_company_enabled` は `false`

1件でも結果が出た場合は、最終migrationへ進みません。自動削除・自動統合はせず、対象行と参照工数を確認します。

### 6. Edge Function

`supabase/functions/invite-company-user` をSupabaseへデプロイします。既定のJWT検証は無効にしません。Function環境へ `APP_ORIGIN` としてHTTPSの本番URLを設定します。`SUPABASE_SERVICE_ROLE_KEY` はSupabase Function環境だけで使用し、ブラウザ・Git・Vercelフロントエンドへ置きません。

### 7. 厳格RLS

`supabase/migrations/202608180002_multi_company_enforce.sql` を実行します。事前条件に違反がある場合は自動的にロールバックします。`not null`化とindex作成を含むため、利用者が入力しないメンテナンス時間に実行します。成功した最後にだけ `multi_company_enabled` が `true` になります。

## 必須テスト

次の4アカウントをテスト環境で用意します。

- A社会社管理者
- A社作業者
- B社会社管理者
- B社作業者

確認項目:

1. A社作業者はA社のマスタだけを候補表示できる。
2. A社作業者は自分の工数だけを閲覧・編集できる。
3. A社会社管理者はA社全員の工数だけを集計・Excel出力できる。
4. A社ユーザーがB社のUUIDをRESTへ直接指定しても取得・更新できない。
5. 作業者が `admin.html`、`companies.html`、`company-users.html` を直接開けない。
6. 会社管理者は別会社の作業者を招待できず、`system_admin` や `company_admin` を作れない。
7. 同じ作業者へ2アカウントを紐づけられない。
8. A社とB社で同じ作業者名・作業内容名・生産番号を別々に登録できる。
9. 会社をまたぐ作業者・生産番号・作業内容を組み合わせた工数登録がDBで拒否される。
10. 招待メールからパスワードを設定し、所属会社の権限でログインできる。

## 本番適用

テスト合格後、利用者が入力しない時間帯を決めます。最新バックアップを取得し、フロントエンドとEdge Functionを準備してから、テスト環境と同じ順番で1社目だけ移行します。確認結果を保存し、既存管理者・作業者の入力、履歴、集計、Excelを確認してから2社目を追加します。

## 障害時

- 最終migrationが失敗した場合はトランザクションで元に戻るため、エラーを記録して原因を直します。
- 適用後に異常が出た場合は新規入力を止め、`BETA_BACKUP_RECOVERY.md` に従って別プロジェクトで復元確認します。
- 2社目のデータが存在する状態で機能フラグだけを `false` に戻すと、フロント側の会社絞り込みが解除されるため行いません。RLSとフロントをセットで復旧します。
- 本番で行を直接削除したり、会社IDを一括上書きしたりしません。
