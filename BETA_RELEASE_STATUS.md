# 工数管理アプリ β版公開状況

更新日: 2026-08-04

## 判定

**条件付きでβ版公開可能**

1社・3〜5人・14日間に限定し、下記の公開前必須項目を完了すれば開始できます。複数会社への提供は、会社分離RLSの移行完了まで不可です。

## 実装・確認済み

- Supabase Authによるログイン。
- 管理者と作業者の画面権限分離。
- 未ログインの直接URLアクセス保護。
- 作業者本人だけの工数参照・更新をDBのRLSで制限。
- 匿名RESTアクセス拒否。
- 実メールのパスワード再設定リンクと再ログイン。
- 製番、作業内容、作業者の本番用マスタ整理。
- 請求・単価機能を工数管理版の通常導線から非表示。
- 390px幅で主要4画面にページ全体の横はみ出しがないことを確認。
- 保存・更新の処理中ボタン無効化と再送防止。
- 保存時と編集時の時間帯重複警告。
- 重複確認通信に失敗した場合は保存・更新を中止。
- 更新・削除対象が0件の場合に成功扱いしない。
- Excel出力0件時の案内。
- Excel出力前に集計を更新し、画面と同じデータ集合からファイルを生成。
- ログイン後リダイレクト先を同一オリジンの許可画面へ限定。
- 主要操作を保存する監査ログ追加SQLを用意。
- 新規・更新データをDB側でも検証するNOT VALID制約SQLを用意。
- バックアップと別環境復元の手順を文書化。
- β安全修正と追加ドキュメントを元リポジトリ `/Users/katomikihiko/kosu-app` へ反映。
- ローカルコミットを作成。GitHub pushは認証情報未設定で未完了。
- 生産番号の正規化共通関数、作業者による候補検索・仮登録、管理者の未確認確認・統合導線を元リポジトリへ反映。
- `SUPABASE_SEIBAN_PRODUCTION_NUMBER_SETUP.sql` を追加。`seiban_key` UNIQUE制約、`pending/confirmed`、登録者・確認者、統合RPCを含む。
- DB変更前に実行する読み取り専用の `SUPABASE_SEIBAN_PRODUCTION_NUMBER_CONFIRM_ONLY.sql` を追加。

## 公開前必須

- [ ] ローカルコミットを `origin/main` へpushし、Vercelへデプロイする。
- [ ] デプロイ前に `SUPABASE_SEIBAN_PRODUCTION_NUMBER_SETUP.sql` の重複確認クエリを実行し、重複0件を確認してから本番へ適用する。
- [ ] 先に `SUPABASE_SEIBAN_PRODUCTION_NUMBER_CONFIRM_ONLY.sql` を実行し、空キー0件・重複0件を確認する。
- [ ] 生産番号の表記揺れ、作業者仮登録、管理者確認、統合を本番DB適用後に確認する。
- [ ] デプロイ後、管理者・作業者の両方で保存、編集、権限制御を再確認する。
- [ ] DBバックアップ取得後、`SUPABASE_BETA_AUDIT_SETUP.sql` をテスト環境で確認してから本番へ適用する。
- [ ] `SUPABASE_BETA_VALIDATION_SETUP.sql` をテスト環境で確認し、既存データ違反がないことを確認してから本番へ適用する。
- [ ] Supabaseの現在プランと `Database > Backups` の利用可否・最新時刻を確認する。
- [ ] iPhone Safari実機で入力、編集、キーボード表示、ボタン操作を確認する。
- [ ] Android Chrome実機、または利用会社がAndroidを使わないことを確認する。
- [ ] β利用者3〜5人のAuthユーザーと `user_profiles` を作成する。
- [ ] β開始直前の全期間明細ExcelとDB論理バックアップを保存する。

## β期間中の制約

- 利用会社は1社だけ。
- 管理者権限 `system_admin` は信頼できる管理者1名だけに付与する。
- 作業者による削除は不可。削除は管理者だけが確認後に実行する。
- 2社目の登録は `MULTI_COMPANY_MIGRATION_PLAN.md` 完了後。
- 毎営業日終了後に当日分の明細Excelを保存する。
- 問題発生時は入力を止め、時刻・ユーザー・対象工数を記録する。

## 未実装・第2段階

- 全マスタを含む会社単位RLS。
- `company_admin` のフロントエンド対応。
- 工数の論理削除・画面からの復元。
- 自動バックアップ検証と定期復元訓練。
- 請求書・請求連携。
