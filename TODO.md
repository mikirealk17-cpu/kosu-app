# TODO

## 完了

- デザイン案1と2を融合して、工数入力・入力履歴編集・集計画面へ反映
- スマホ向けに入力欄・ボタン・固定保存ボタンを調整
- 作業者管理、作業内容管理、製番管理を追加
- 元請け管理の土台を追加し、工数入力ごとに元請けを保存
- 元請け別の集計タブとCSVを追加
- 請求確認CSVに小数時間、単価、金額欄を追加
- 単価マスタを追加
- 工数入力画面に元請け、単価区分を追加
- 工数保存時に単価を1件だけ判定し、保存時点の単価情報を保存
- 履歴編集で元請け、単価区分を編集し、単価を再判定
- 請求確認CSVで時間単価、固定単価、請負単価の金額を出力
- 請負単価は同じ元請け、製番ごとにCSV内で1回だけ計上
- 単価自動適用用のSupabase SQLを分離
- 単価自動適用の設定手順書を追加
- 単価自動適用のテストシナリオを追加
- Supabase SQL Editorで `SUPABASE_RATE_SETUP.sql` を実行する
- `BILLING_RATE_SETUP_GUIDE.md` の順番に沿って設定する
- 元請けを登録する
- 時間単価、固定単価、請負単価を登録する
- `BILLING_RATE_TEST_SCENARIOS.md` の内容で時間単価、固定単価、請負単価の計算を確認する
- 同じ条件の単価がSupabase側の制約で重複登録できないことを確認する
- 単価未設定の組み合わせでは工数が保存されないことを確認する
- 請求確認CSV相当の計算で請負単価が製番ごとに1回だけ計上されることを確認する
- テスト用に追加した固定単価、請負単価、工数データを整理する
- 大元請けを入力画面、履歴編集、単価管理、請求確認CSVから外す
- 集計画面から設備別、作業者日別、作業者別設備CSVを外し、CSV出力を明細・表示中集計・請求確認に整理する
- ログイン・権限管理の設計書を追加する
- ログイン・権限管理用のSupabase SQL案を追加する
- 次チャット用の引き継ぎメモを追加する
- 工数管理版として一旦商品化するため、単価管理と請求確認CSVを通常導線から外す
- 工数入力画面と履歴編集画面から元請け・単価区分を通常入力項目として外す
- 未追跡の空ファイル `main` を削除する
- 2026-06-26〜2026-07-03の実データ8件をSupabase RESTで取得し、CSV対象データが読めることを確認する
- 公開キーで `rate_master` と `work_logs` の金額系列が読めることを確認する
- CSV/Excel出力の先頭に、集計タイトル・期間・絞り込み条件を追加する
- 作業者・作業内容・製番で絞り込んだ出力ファイル名に、選択条件を入れる
- 集計画面のファイル出力欄に、出力予定のタイトル・形式・期間・絞り込み条件を表示する
- 工数保存後に、直近で保存した日付・作業者・製番・設備名・時間・実働時間を表示する
- 保存後の直近入力表示を、画面上部の大きめの確認カードとして表示する
- 保存後の直近入力表示を、画面移動ボタンを塞がない画面下側表示に変更する

## 次に確認すること

### 優先度高

- [完了] Supabase SQL Editorで `SUPABASE_WORK_TRACKING_RELEASE_SECURITY.sql` を実行し、公開キーから金額系データを読めない状態にする
- [完了] SQL実行後、公開キーで `rate_master` と `work_logs.rate_type` / `rate_master_id` / `unit_price` / `billing_amount` が読めないことを確認する
- [一部完了] SQL実行後、工数の追加・更新・削除、履歴画面、集計画面が壊れていないことを確認した。CSV実ファイルの目視確認は継続
- [一部完了] 明細CSVと表示中集計CSVの生成内容を実データで確認した。ブラウザの出力ボタンはエラーなく `出力しました` になるが、PCブラウザ保存後の実ファイル目視確認は継続
- [一部完了] 明細Excelと表示中集計Excelの生成内容を実データで確認し、列幅指定が入ることを確認した。表計算ソフトで開く実ファイル目視確認は継続
- 工数保存後の直近入力表示を、実データ保存で確認する
- iPhone実機で工数入力、集計、履歴編集の横はみ出しを確認する

### 優先度中

- [一部完了] 工数入力、履歴編集、削除、集計表示、作業者追加、作業内容追加、製番追加を確認した。CSV実ファイル確認は継続
- [完了] 本番DBで確認用の作業者・作業内容・製番を追加し、編集、工数入力、工数更新、集計対象取得、工数削除、確認用データ整理まで確認する
- [完了] 集計画面に、列幅付きで見やすい明細Excelと表示中集計Excelの出力を追加する
- [完了] CSV/Excel出力から `実働分` 列を削除する
- [完了] iPhone実機で元請け・単価区分の隠しselectが表示される問題を修正し、スマホ幅390pxで非表示を確認する
- [完了] iPhone実機でキーボード表示中に保存ボタンがちらつく問題を修正する
- [完了] スマホ時の保存ボタン固定表示を解除し、キーボード表示中に保存ボタンが大きく動く原因を除去する
- [完了] 2026-07-01〜2026-07-13の本番データ2件で、明細CSV/Excelと表示中集計CSV/Excelの列構成を確認する
- [完了] 保存ボタン固定解除後、ブラウザのスマホ幅390pxで工数入力、履歴編集、集計にページ全体の横はみ出しがないことを再確認する
- [完了] ブラウザのスマホ幅390pxで、工数入力、履歴編集、集計、管理画面にページ全体の横はみ出しがないことを確認する
- [完了] 第1段階では元請け管理画面を通常導線から外す
- [完了] 第1段階では集計画面の元請け別タブを通常導線から外す

### 優先度低

- `SUPABASE_RATE_SETUP.sql` を再実行し、元請けマスタ・単価マスタの大元請け必須を解除する
- `AUTH_PERMISSION_DESIGN.md` の権限区分が実運用に合うか確認する
- `SUPABASE_AUTH_PERMISSION_SETUP.sql` を実行する前に、会社単位と作業者単位の閲覧範囲を確認する
- Supabase Authのログイン方式を決める

## 将来拡張

- Supabase Authによるログイン
- `SUPABASE_AUTH_PERMISSION_SETUP.sql` の実行
- 既存作業者と既存工数への会社ID付与
- 会社管理者、作業者、システム管理者の権限分離
- 請求済みフラグ
- 履歴編集で元請け、作業者、製番、単価区分を変更した時の単価再判定
- 請求書番号
- CSV出力履歴
- 請求年月
- 請求書PDF出力
- 端数処理の切り替え
  - 四捨五入
  - 切り捨て
  - 切り上げ
