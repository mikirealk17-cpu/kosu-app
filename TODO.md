# TODO

## 優先度 高

- Supabase側で `SUPABASE_SETUP.sql` を実行する
- Supabase側で `worker_master` が取得できることを確認する
- Supabase側で `work_logs.worker_id` が保存できることを確認する
- 実データで工数保存を確認する
- 作業者管理の追加・編集・削除を実データで確認する

## 優先度 中

- スマホUI改善

## 優先度 低
